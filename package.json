{
  "name": "transfer-wire-server",
  "version": "1.0.0",
  "description": "Small proxy server that holds your API-Football key privately and serves recent transfer data to the Transfer Wire frontend.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
